node_modules/
dist/
*.log
.DS_Store
.nexus-agent/
backend/.nexus-agent/
*.tsbuildinfo
.env
.env.local

# Rust / Tauri build artifacts (embed absolute build paths — never publish).
target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/nexus-agent-runtime*

# Local tooling / editor state (may contain machine-specific absolute paths).
.qoder/
.claude/
.idea/
.vscode/

# The brand denylist lives OUT of the repo (so the reproduced product's own
# brand/codename/logo-mark strings are never published here). CI materializes it
# from the BRAND_DENYLIST secret; it is excluded from the scan itself (§11).
.brand-denylist
